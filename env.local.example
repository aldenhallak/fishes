# Fish Art 环境变量配置示例
# 复制此文件为 .env.local 并填入真实的配置信息

# ==================== Supabase 配置 ====================
# Supabase 项目URL和密钥（用于认证）
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT密钥（用于Hasura认证）
SUPABASE_JWT_SECRET=your-jwt-secret-here

# ==================== Hasura 配置 ====================
# Hasura GraphQL端点
HASURA_GRAPHQL_ENDPOINT=https://your-hasura-project.hasura.app/v1/graphql
HASURA_ADMIN_SECRET=your-hasura-admin-secret

# ==================== Redis 配置 ====================
# Redis连接（用于战斗系统缓存）
REDIS_URL=redis://localhost:6379
# 或使用 Upstash Redis
# UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token

# ==================== 七牛云配置 ====================
# 七牛云密钥（用于图片存储）
QINIU_ACCESS_KEY=your-qiniu-access-key
QINIU_SECRET_KEY=your-qiniu-secret-key

# 七牛云存储空间
QINIU_BUCKET=fish-art
QINIU_BASE_URL=https://your-cdn-domain.com
# 七牛云存储目录（基础路径，子目录按分类自动创建）
QINIU_DIR_PATH=fishart_web/
# 图片将按分类存储在以下子目录：
#   fishart_web/fish/      - 鱼作品图片
#   fishart_web/avatars/   - 用户头像
#   fishart_web/battle/    - 战斗相关图片
#   fishart_web/temp/      - 临时文件
QINIU_ZONE=Zone_z2

# ==================== 部署配置 ====================
# Node环境
NODE_ENV=development

# API Base URL（可选，用于CORS）
NEXT_PUBLIC_API_URL=http://localhost:3000

# ==================== 后端数据源配置 ====================
# 全局后端选择：hasura 或 original
# hasura - 使用自建Hasura数据库（推荐，包括鱼数据和鱼缸）
# original - 使用原作者后端（临时方案，用于快速测试）
BACKEND_TYPE=hasura

# 原作者后端URL（仅在BACKEND_TYPE=original时使用）
ORIGINAL_BACKEND_URL=https://fishes-be-571679687712.northamerica-northeast1.run.app

# ==================== 其他配置 ====================
# 日志级别
LOG_LEVEL=info

# ==================== 登录模式配置 ====================
# 登录模式：AUTO（自动登录） 或 MANUAL（手动登录，默认）
# 当设置为 AUTO 时，系统会自动使用 DEF_USER 和 DEF_PASS 登录，无需手动输入
# 当设置为 MANUAL 或未设置时，用户需要手动登录
LOGIN_MODE=MANUAL

# 默认用户账号（用于自动登录模式）
# 当 LOGIN_MODE=AUTO 时，系统会自动使用此账号登录
# 格式：邮箱地址
DEF_USER=test@example.com

# 默认用户密码（用于自动登录模式）
# 当 LOGIN_MODE=AUTO 时，系统会自动使用此密码登录
DEF_PASS=test123456

