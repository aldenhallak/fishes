# Railway Nixpacks 配置
# 强制使用 npm install 而不是 npm ci

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
dependsOn = ["setup"]
cmds = ["npm install --legacy-peer-deps"]

[start]
cmd = "npm run start"

